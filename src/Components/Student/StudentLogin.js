import React from "react";
import Login from "../Login";

const StudentLogin = () => {
    return ( <
        div >
        <
        h2 > Student Login < /h2> <Login / >
        <
        /div>
    );
};

export default StudentLogin;